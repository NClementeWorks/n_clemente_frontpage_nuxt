export { };

declare global {

  type ElementScreenProperties = {
    x : Ref<number>,
    y : Ref<number>,
    top : Ref<number>,
    left : Ref<number>,
    width : Ref<number>,
    height : Ref<number>,
  }

}